<<h1>All Tags</h1>

<ul id="tags">
  <li>
    <% @tags.each do |tag| %>
      <%= link_to tag.name, tag_path(tag) %><br/>
    <% end %>
  </li>
</ul>

<%= link_to "<< Back to Articles List", articles_path %>
